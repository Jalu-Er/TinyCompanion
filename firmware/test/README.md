# TinyCompanion Firmware Tests

This directory is reserved for unit tests. Tests can be run locally using the native test environment (configured in `platformio.ini` if needed) to validate business logic modules ( FSM, EmotionEngine) on a PC.

## Executing Tests:
```bash
pio test -e uno
```
