# Tools Folder

This directory contains development scripts, asset converters, code formatters, and deployment automations.

## Planned Utilities:
*   `bitmap_converter.py` - Script to convert graphic files into C++ PROGMEM arrays (if bitmap rendering is needed).
*   `test_runner.py` - Local simulation runner for verifying FSM state changes on desktop hosts.
